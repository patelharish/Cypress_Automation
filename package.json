{
  "name": "cypressautomation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cypress:test": "npx cypress run",
    "test": "echo \"Error: no test specified\" && exit 1",
    "chrome:test": "npx cypress run --browser chrome",
    "firefox:test": "npx cypress run --browser firefox",
    "open": "npx cypress open"
  },
  "author": "harish patel",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.4.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "dependencies": {
    "nodemailer": "^7.0.3"
  }
}
